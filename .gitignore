# Reference checkout — upstream n8n, not part of this repository
n8n-master/

# Generated runtime secrets (created by setup.sh) — never commit
.env

# Internal/compliance scratch — never publish (e.g. internal license notes)
files/

# OS / editor artifacts
.DS_Store
Thumbs.db
*.swp
